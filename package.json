{
  "name": "jot",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:mobile": "pnpm --filter mobile start",
    "start:backend": "uvicorn app.main:app --reload --app-dir packages/backend"
  }
}
